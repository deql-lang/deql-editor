// Re-export layout from @deql-lang/core (renamed for backward compatibility)
export { applyLayout as applyElkLayout, NODE_DIMS } from '@deql-lang/core';
export type { GroupBounds, LayoutResult } from '@deql-lang/core';
